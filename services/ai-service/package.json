{
  "name": "@agritrace/ai-service",
  "version": "1.0.0",
  "description": "AI yield prediction service for AgriTrace AI",
  "scripts": {
    "dev": "uvicorn main:app --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "pytest"
  }
}
