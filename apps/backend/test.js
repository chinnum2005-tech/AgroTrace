const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { try { await prisma.$connect(); const count = await prisma.user.count(); console.log('Count:', count); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } test();
