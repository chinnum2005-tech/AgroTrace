const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://neondb_owner:npg_tT5wEBhfdxl9@ep-super-shadow-amte8gib.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&connect_timeout=30' } } }); async function test() { try { await prisma.$connect(); const count = await prisma.user.count(); console.log('Count:', count); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } test();
